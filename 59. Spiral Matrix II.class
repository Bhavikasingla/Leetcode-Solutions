Ques: Given a positive integer n, generate an n x n matrix filled with elements from 1 to n2 in spiral order.

Example 1:
Input: n = 3
Output: [[1,2,3],[8,9,4],[7,6,5]]

Example 2:
Input: n = 1
Output: [[1]]
 
Constraints:
1 <= n <= 20

Solution:
public class Solution {
    public int[][] generateMatrix(int n) {
        int[][] rs = new int[n][n];
        int t = 0,b = n-1,l = 0,r = n-1;
        int num = 1;
        
        while(l<=r && t <=b){
            for(int i=l;i<=r;i++){
                rs[t][i] = num++;
            }
            t++;
            for(int i= t;i<=b;i++){
                rs[i][r] = num++;
            }
            r--;
            for(int i= r;i>=l;i-- ){
                rs[b][i] = num++;
            }
            b--;
            for(int i = b;i>=t;i--){
                rs[i][l] = num++;
            }
            l++;
        }
        return rs;
    }
}
